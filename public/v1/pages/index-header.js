// Register `phoneList` component, along with its associated controller and template
angular.
module('App12ships').
component('indexHeader12', { // This name is what AngularJS uses to match to the `<phone-list>` element.

    templateUrl: 'index-header12.html',

    controller: function() {
        var ctrl = this;
    }
});
